name: Azure-RDS-Deployment

on:
  push:
    branches:
      - main

  pull_request:
    branches:
      - main

  workflow_dispatch:

permissions:
  id-token: write
  contents: read

env:
  TF_VERSION: "1.8.5"

jobs:

  ##################################################
  # Terraform Validation
  ##################################################

  terraform-validation:

    name: Terraform Validation
    runs-on: ubuntu-latest

    steps:

      ##################################################
      # Checkout Repository
      ##################################################

      - name: Checkout Repository
        uses: actions/checkout@v4

      ##################################################
      # Azure Login using OIDC
      ##################################################

      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      ##################################################
      # Setup Terraform
      ##################################################

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TF_VERSION }}

      ##################################################
      # Create Terraform Plugin Cache
      ##################################################

      - name: Create Terraform Plugin Cache
        run: |
          mkdir -p /tmp/terraform-plugin-cache

      ##################################################
      # Terraform Format
      ##################################################

      - name: Terraform Format
        working-directory: RDS-Project/terraform
        env:
          TF_PLUGIN_CACHE_DIR: /tmp/terraform-plugin-cache
        run: terraform fmt -recursive

      ##################################################
      # Terraform Init
      ##################################################

      - name: Terraform Init
        working-directory: RDS-Project/terraform/environments/poc
        env:
          TF_PLUGIN_CACHE_DIR: /tmp/terraform-plugin-cache
        run: terraform init

      ##################################################
      # Terraform Validate
      ##################################################

      - name: Terraform Validate
        working-directory: RDS-Project/terraform/environments/poc
        env:
          TF_PLUGIN_CACHE_DIR: /tmp/terraform-plugin-cache
        run: terraform validate

  ##################################################
  # Terraform Plan
  ##################################################

  terraform-plan:

    name: Terraform Plan
    runs-on: ubuntu-latest

    needs: terraform-validation

    steps:

      ##################################################
      # Checkout Repository
      ##################################################

      - name: Checkout Repository
        uses: actions/checkout@v4

      ##################################################
      # Azure Login using OIDC
      ##################################################

      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      ##################################################
      # Setup Terraform
      ##################################################

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TF_VERSION }}

      ##################################################
      # Create Terraform Plugin Cache
      ##################################################

      - name: Create Terraform Plugin Cache
        run: |
          mkdir -p /tmp/terraform-plugin-cache

      ##################################################
      # Terraform Init
      ##################################################

      - name: Terraform Init
        working-directory: RDS-Project/terraform/environments/poc
        env:
          TF_PLUGIN_CACHE_DIR: /tmp/terraform-plugin-cache
        run: terraform init

      ##################################################
      # Terraform Plan
      ##################################################

      - name: Terraform Plan
        working-directory: RDS-Project/terraform/environments/poc
        env:
          TF_PLUGIN_CACHE_DIR: /tmp/terraform-plugin-cache
        run: terraform plan -out=tfplan

      ##################################################
      # Upload Terraform Plan
      ##################################################

      - name: Upload Terraform Plan
        uses: actions/upload-artifact@v4
        with:
          name: terraform-plan
          path: RDS-Project/terraform/environments/poc/tfplan

  ##################################################
  # Terraform Apply
  ##################################################

  terraform-apply:

    name: Terraform Apply
    runs-on: ubuntu-latest

    needs: terraform-plan

    if: github.ref == 'refs/heads/main'

    steps:

      ##################################################
      # Checkout Repository
      ##################################################

      - name: Checkout Repository
        uses: actions/checkout@v4

      ##################################################
      # Azure Login using OIDC
      ##################################################

      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      ##################################################
      # Setup Terraform
      ##################################################

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TF_VERSION }}

      ##################################################
      # Create Terraform Plugin Cache
      ##################################################

      - name: Create Terraform Plugin Cache
        run: |
          mkdir -p /tmp/terraform-plugin-cache

      ##################################################
      # Terraform Init
      ##################################################

      - name: Terraform Init
        working-directory: RDS-Project/terraform/environments/poc
        env:
          TF_PLUGIN_CACHE_DIR: /tmp/terraform-plugin-cache
        run: terraform init

      ##################################################
      # Terraform Apply
      ##################################################

      - name: Terraform Apply
        working-directory: RDS-Project/terraform/environments/poc
        env:
          TF_PLUGIN_CACHE_DIR: /tmp/terraform-plugin-cache
        run: terraform apply -auto-approve
